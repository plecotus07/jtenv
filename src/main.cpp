// +++ -------------------------------------------------------------------------
#include "mvcapp_jtenv.hpp"
// +++ -------------------------------------------------------------------------
int main (int aArgc, char* aArgv[])
{
	jtenv::MvcApp app {aArgc, aArgv};

	return (app.run() ? 0 : 1);
}
// +++ -------------------------------------------------------------------------
